#!/usr/bin/env bash


# Import Current Theme
DIR="$HOME/.config/rofi"
RASI="$DIR/layouts/askpass.rasi"

# Rofi text dialog to get password
rofi -dmenu \
     -password \
     -i \
     -p "Root" \
     -theme ${RASI}